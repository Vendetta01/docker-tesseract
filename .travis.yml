language: python

services:
  - docker

install:
  - docker build -t docker-tesseract .

script:
  - docker run --user `id -u`:`id -g` -v `pwd`/test:/exchange docker-tesseract eng_bw-png test_output txt
  - docker run --user `id -u`:`id -g` -v `pwd`/test:/exchange docker-tesseract eng_bw-png test_output pdf
